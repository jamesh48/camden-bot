import { Box, Typography } from "@mui/material";

const CamdenBotTitle = () => {
  return (
    <Box sx={{ display: "flex", justifyContent: "center" }}>
      <Typography variant="h2">Camden Apartment Finder</Typography>
    </Box>
  );
};

export default CamdenBotTitle;
